---
- name: "Hyprland | {{ ansible_distribution }} | Install Hyprland"
  community.general.pacman:
    name:
      - hyprcursor
      - hyprlang
      - hyprutils
      - hyprwayland-scanner
      - hyprland
      - xdg-desktop-portal-hyprland
    state: present
  become: true

- name: "Cursor Icons | {{ ansible_distribution }} | Install Cursor Icons"
  kewlfft.aur.aur:
    name: "{{ icons.cursor_icons_theme }}"
    state: present
  become: false
